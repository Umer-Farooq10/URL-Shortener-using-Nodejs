<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup Page</title>
    <style>
        body {
            font-family: 'Nobile', Helvetica, Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
        }

        h1 {
            color: #3F3F3F;
            font-family: 'Corben', Georgia, Times, serif;
            font-size: 48px;
            margin-bottom: 32px;
            text-align: center;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
            border: 1px solid #3F3F3F;
            padding: 30px;
            border-radius: 5px;
            box-shadow: 1px 1px 5px rgba(0, 0, 0, 0.2);
            width: 300px;
            margin: 0 auto;
            background-color: #fff;
            /* box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); */
        }

        label {
            color: #3F3F3F;
            font-size: 16px;
            margin-bottom: 4px;
            display: block;
            align-self: flex-start;
        }

        input, select {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border-radius: 5px;
            border: 1px solid #3F3F3F;
            background-color: #e5e5e5;
            font-size: 18px;
        }

        input[type="submit"], a {
            background-color: #3F3F3F;
            color: #FFFFFF;
            font-size: 18px;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
        }

        input[type=" submit"]:hover, a:hover {
            background-color: #171717;
        }

        select {
            appearance: none;
            -webkit-appearance: none;
            -moz-appearance: none;
            background-image: url("data:image/svg+xml;utf8,<svg fill='%233E3E3E' height='24' viewBox='0 0 20 20' width='20' xmlns='http://www.w3.org/2000/svg'><path d='M9.293 12.95l.707.707L15.657 8l-1.414-1.414L10 10.828 5.757 6.586 4.343 8z'></path></svg>");
            background-repeat: no-repeat;
            background-position: right 10px center;
            background-size: 12px;
            padding-right: 35px;
        }

        footer {
            text-align: center;
            font-size: 14px;
            color: #3F3F3F;
            margin-top: 50px;
        }

        footer a {
            color: #3F3F3F;
            text-decoration: none;
        }

        main {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        @media (min-width: 768px) {
            main {
                flex-direction: row;
                justify-content: center;
            }

            form {
                width: 300px;
            }

            aside {
                margin-left: 50px;
            }
        }
    </style>
</head>
<body>
    <h1>Signup form</h1>
    <form action="/api/users/create" method="post">
        <label for="username">Username:</label>
        <input type="text" id="username" name="username">
        <label for="email">Email:</label>
        <input type="email" id="email" name="email">
        <label for="password">Password:</label>
        <input type="password" id="password" name="password">
        <select name="role">
            <option value="normal" selected>Normal</option>
            <option value="admin">Admin</option>
        </select>
        <input type="submit" value="Signup">
        <a href="/api/users/login">Login</a>
    </form>
</body>
</html>
